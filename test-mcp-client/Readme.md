## Test MCP Client

- Run the `fastapi-mcp-api` as server.
- Navigate to the `fastapi-mcp-api` directory.
- And execute the below command to run the server.

```sh
uvicorn main:app --reload
```

- Then run the below command to execute the client.

```sh
python main.py
```